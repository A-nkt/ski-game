# Codexへの指示: 爽快感コアの再構築（プロデューサーレビュー第2弾）

前回タスクのP0（コンボ・フィーバー・ゴールドコイン）は実装を確認済み。良い仕事だった。
しかしプロデューサーとして認めなければならない: **前回の指示は「もう一回遊ぶ理由」を
作るものであって、「遊んでいる10秒間が気持ちいい」を作るものではなかった。**
世界最高クラスに必要なのは後者が先だ。今回はゲームの心臓部に手を入れる。

## 診断: なぜ今のゲームは「爽快」ではないのか

1. **速度が実力と無関係** — `baseSpeed = Math.min(49, 27 + distance * .012)`。
   速度は距離の関数でしかなく、どれだけ美しくカービングしても1m/sも速くならない。
   スキーの爽快感の本体は「上手い滑り→加速」のループであり、これが無い限り
   コンボもフィーバーも飾りにしかならない。**本タスクの最重要項目。**
2. **体感速度の演出がゼロ** — FOVは66固定。HUDの数字が176km/hでも、
   画面から伝わる速度は97km/hのときと同じ。
3. **地形が飾り** — `terrainHeight()` に起伏はあるが、プレイヤーは常に地面に
   吸い付いており、クレスト（尾根）で自然に飛ぶことができない。
4. **障害物が一様乱数** — ライン取りのリズム（スラロームの律動）が生まれず、
   反射的な回避の連続にしかならない。フロー状態に入れない。
5. **一撃死のみ** — 3分間積み上げたランが、かすり傷1回で即終了する。
   高速域に到達した「一番気持ちいい時間」が最も短命という構造的欠陥。

対象ファイル: `src/game3d.js`（メイン）、必要に応じ `index.html` / `styles.css`

---

## G0-1: スキル→スピード直結ループ【最優先。これが本タスクの核】

速度計算を「ベース + 稼いだブースト」の2階建てにする。
`boost` 変数（0〜12 m/s、常時減衰: `boost *= Math.pow(.6, dt)` 程度）を導入し、
`speed = (baseSpeed + boost) * (1 - |edgeAngle| * .045) * feverFactor` とする。

- **カービングパンプ**: エッジ荷重状態（`|edgeAngle| > .5` が0.4秒以上継続）から
  ニュートラル（`|edgeAngle| < .15`）を通過した瞬間に `boost += 2.5`。
  S字を深く刻むほど加速する＝実際のスキーのパンピング。切り返しの瞬間に
  短い「シュッ」というSE（既存 `noiseBurst` 流用、高め周波数）と雪煙の増量を添える。
  **これにより左右操作が「回避」から「加速手段」に変わる。ゲームの発明ポイント。**
- **タック（直滑降）**: `↓` / `KeyS` 長押しで目標速度+18%、操舵応答半減、
  `crouchAmount` を0.7へ深め、風音を開放。障害物の多い区間で使えば死ぬ。
  リスクの自己選択。`keys` オブジェクトと `setKey()` に追加。
- **クリーン着地**: `|edgeAngle| < .15` で着地したら `boost += 3` ＋「PERFECT LANDING」
  トースト。カービング中の着地は速度15%スクラブ＋大きめのshake。
  ジャンプが「タイミングを合わせて真っ直ぐ降りる」スキルチェックになる。
- **ニアミス加速**: `registerNearMiss()` に `boost += 1.2` を追加。
  危険なラインが文字通り速い、へ。
- **上限開放**: baseSpeedのキャップを49→55に引き上げ。boost込み最大約67m/s
  （約240km/h）。反応時間が縮むこと自体が自然な難易度曲線になるので、
  障害物の密度調整は不要（まず現状密度で遊んで判断する）。

**完了条件**: 何も考えず直進するプレイヤーと、S字パンプ＋タック＋ニアミスを
決め続けるプレイヤーで、巡航速度に体感できる差（+20%以上)がつくこと。

## G0-2: 体感速度の演出

- **速度連動FOV**: `fov = 66 + THREE.MathUtils.clamp((speed - 27) / 40, 0, 1) * 9`
  （66→75）をゆっくりlerp（`1 - Math.pow(.5, dt)` 程度）。変化量が0.01超の
  フレームのみ `updateProjectionMatrix()` を呼ぶ。
  ※前回「カメラ不可侵」と指示したが、プロデューサー判断で柱を更新する。
  酔いの原因はロール・ランダム揺れ・ズームの脈動であり、緩やかな単調FOV変化は
  別物（レースゲーム全般の標準技法）。ロール/揺れ/脈動の禁止は引き続き厳守。
- **スピードライン**: `speed > 40` でフェードインする流線パーティクル層を追加。
  カメラ近傍の視界周辺部に細長い粒子（既存snowPointsと同じBufferGeometryパターン、
  z速度を雪の2.5倍に）。共有ジオメトリで実装。
- **風音の第2層**: `speed > 45` でハウリング系のbandpassノイズ（1800Hz付近を
  ゆっくりスイープ）をフェードイン。既存のwindGain/windFilterの隣に追加。
- **モバイル触覚**: `navigator.vibrate?.()` を着地20ms / ニアミス30ms / クラッシュ80ms。

**完了条件**: HUDを隠した状態でも「今めちゃくちゃ速い」が映像と音だけで分かること。

## G1-1: 地形で飛ぶ（クレストジャンプ）

- `terrainHeight()` に低頻度・高振幅のローラー起伏項を1つ追加
  （例: `Math.max(0, Math.sin(worldZ * .006 + 2.2)) ** 3 * 2.4` のような
  「たまに来る尾根」）。
- 接地中、地面の降下速度が自機の落下より速いクレストでは自然に空中へ:
  毎フレーム `groundY` の変化率を追跡し、非ジャンプ中に
  `(groundYの降下速度) < -6 m/s` かつ `speed > 38` なら
  `jumpVelocity = 地形由来の初速` で空中状態へ移行する。
- 既存のBIG AIR/エアタイムボーナス（前回P1-1）と自動的に噛み合う。
  地形を読んで尾根でタック→大ジャンプ→クリーン着地ブースト、が最高速ルートになる。

**完了条件**: ランプを踏まなくても、速度が乗った状態で尾根を越えると自然に飛ぶ。

## G1-2: ラインのある障害物配置

`populate()` を一様乱数から**パターンライブラリ抽選**に変える。1パターン=30〜60m:

- **スラロームゲート**: 木のクラスタを左右交互に配置し、S字を強制する区間。
  → G0-1のパンプと直結し「リズムに乗ると加速する」体験を生む。
- **レーシングラインコイン**: コインを旋回弧に沿ったサイン軌道で配置し、
  「速いライン」をコインが教える。コイン=お小遣いではなく**理想ラインの可視化**へ。
- **現状の散布**（NORMAL区間として残す）
- 前回P1-2のゾーン制と統合してよい（ゾーン=パターンの出現重み変更として実装）。

**完了条件**: スラローム区間でリズムよく切り返すと、コインが自然に集まりながら
加速していく体験が成立すること。

## G2: 死の再設計（かすり＝スタンブル）

- 判定を3層にする: 中心直撃=死 / かすり=スタンブル / その外=ニアミス。
  かすり帯は `dx ∈ [collisionRadius, collisionRadius + .25)`:
  死なずに `speed×0.55`・`boost=0`・大shake・つんのめり姿勢（crouch瞬間加算）・
  鈍いSE。**ニアミスの下限を `collisionRadius + .25` に繰り上げて排他にすること**
  （現状の `dx >= collisionRadius` のままだとスタンブルとニアミスが同時発火する）。
- 直撃死には前回P2のスローモーション（`timeScale` 変数で0.9秒 `dt×0.25`）をここで実装。
  高速域done rightの死は「悔しいが見応えがある」に変わる。

**完了条件**: かすった時に「うわ危なっ」で済んでランが続く。直撃はスローモーで死ぬ。

---

## 優先順位と、前回タスクとの関係

**G0-1 → G0-2 → G1-1 → G1-2 → G2** の順。前回のP1/P2（エアタイム、ゾーン、蓄積要素）で
未着手のものはG1と統合しながら進めてよいが、**G0より先にやることは禁止**。
理由: 報酬システムはコアの快感の「増幅器」であり、増幅する対象（スキルと速度の
ループ）が先に存在しなければならない。

## チューニング指針（体感目標）

| 項目 | 目標 | 意図 |
|---|---|---|
| パンプ1回のブースト体感 | 「音と雪煙で気づく」レベル | 小さすぎると学習されない |
| 上手いプレイヤーの巡航速度差 | 漫然プレイ比 +20%以上 | スキル表現の幅 |
| タック使用可能時間 | 開けた区間で3〜5秒 | 常用させない |
| クレストジャンプ | 30〜60秒に1回 | 「来るぞ…！」の期待 |
| スタンブル | 死の30〜40%を置き換え | ラン寿命を延ばし高速域を長く |

実装後、**自分で5ラン以上プレイし「漫然プレイ」と「全力プレイ」の到達距離・
巡航速度を記録して差を確認**すること。差が出ていなければ数値を再調整。

## 壊してはいけないもの（更新版）

- カメラ: ロール・ランダム揺れ・ズーム脈動は引き続き禁止。
  速度連動の緩やかなFOV変化のみ今回解禁（G0-2の仕様通りに）。
- 既存のコンボ/フィーバー/ゴールドコインの挙動。
- 共有ジオメトリ/マテリアルのパターン（スピードライン層・新パターンも同様に）。
- タイマー・減衰はすべてdt積算/指数減衰で実装（setTimeout禁止）。
- READMEの「ビジュアルとカメラ」節をFOV仕様の更新に合わせて書き換えること。

## 進め方

G0-1完了時点で一度ビルドしてプレイ確認し、速度差が体感できるかを最優先で検証。
以降は1機能ずつ `npm run build` → プレイ確認。G0完了時点で停止しレビューを受けること。

---
---

# 第3弾: 世界を取るための設計（G系完了後に着手。ただし全体像として先に読むこと）

## プロデューサーレビュー: 良作と「世界を取るゲーム」の差

G0〜G2が全部入った時点で、本作は85点の良作になる。だが世界は取れない。理由は4つ。

1. **拡散経路がゼロ** — どれだけ面白くても、面白さは自分では移動しない。
   Wordleが世界を取ったのはパズルの質ではなく「全員が同じ問題を解き、結果を
   絵文字でコピペできた」から。本作には現在、他人に届く仕組みが一切ない。
2. **「顔」がない** — SkiFreeが30年後も語られるのはイエティのおかげ。
   Subway Surfersには警官、Alto'sには夜の静寂がある。本作を友達に一言で
   説明するときの「あの◯◯のゲーム」が存在しない。
3. **音楽がない** — 風とSEだけ。BGMは体験の半分を占める。特に配信・動画クリップ
   経由の拡散では、音が死んでいるゲームはクリップされない。
4. **世界 = 英語圏 + 低スペック端末** — ボイスがja-JP固定。世界のプレイヤーの
   大半は中低スペックのAndroidでプレイする。

## 着手順序（重要: プロデューサー判断）

**G0〜G2 → W2(BGM) → W1(雪崩) → W0(デイリー+シェア) → W3 → W4**

バイラルは一発勝負だ。シェア機能を先に付けても、音楽と「顔」のないゲームが
拡散の瞬間を迎えたら弾を無駄にする。**製品を完成させてから拡散の蛇口を開ける。**

---

## W2: アダプティブBGM（Web Audioステップシーケンサ）

- 外部音源ファイルは使わず、既存の `initAudio()` 体系に**ルックアヘッド型の
  ステップシーケンサ**を追加する（Web Audio標準パターン: `setInterval` 25msで
  `audio.currentTime + 0.1s` 先までノートをスケジュール。ゲームロジックの
  setTimeout禁止ルールとは別枠でよい。これは音声スケジューリングの標準技法）。
- レイヤー構成（すべてオシレータ/ノイズで合成、キーは**Aマイナー**。
  既存SEのコイン880Hz=A5と協和する）:
  - キック+ベース: プレイ中は常時（sine dropとlowpass sawtooth）
  - アルペジオ: コンボ×2以上 or フィーバー中に加算
  - ハイハット(ノイズ16分): タック中に加算
  - 不協和ドローン: 雪崩接近時（W1実装後）
- BPMは120を基準にspeedで150まで連動。フィーバーでフィルタ開放。
- ミュートボタン・masterGainに完全連動。ゲームオーバーで2秒かけてフェード。

**完了条件**: 漫然と滑ると静かで、攻めるほど音が厚くなる。ミュートが完全に効く。

## W1: 雪崩チェイス（このゲームの「顔」）

- ラン開始400m後方から雪崩が追走。基本速度はプレイヤー巡航速度の約0.97倍
  （＝上手く滑れば引き離せる）。**スタンブル・強エッジの減速で差が縮まる**。
  G2のスタンブルに実質コストを与える装置でもある。
- 背後は画面に映らないので、プレゼンスは音と気配で作る（実装コストが低い割に
  恐怖効果が大きい）:
  - 距離150m以内: 低周波ランブル（ノイズ+lowpass 60-120Hz）が距離反比例で増大
  - 60m以内: HUDに距離ゲージ出現、画面端が白く煙る（CSSクラス `.danger`）、
    モバイル振動、W2の不協和ドローン加算
  - 20m以内: 画面左右端に白い雪煙の柱（共有ジオメトリのパーティクル2本）
  - 0m: ゲームオーバー。WIPE OUTではなく専用の「AVALANCHE」画面
- **危険=報酬の整合**: 雪崩60m圏内ではコイン・ニアミスボーナス2倍。
  わざと引きつけて稼ぐ、という上級者の遊びが生まれる（配信映えの本体はここ）。
- 逃げ切りの緩急: 300mごとに雪崩が一時的に減速する「息継ぎ」を入れ、
  常時プレッシャーで疲労させない。

**完了条件**: 音と画面端だけで背後の恐怖が伝わる。捕まる直前に稼ぐ動機が機能する。

## W0: デイリーラン + 絵文字シェア（拡散装置。Wordle構造の移植）

- **シード付き乱数**（mulberry32等の小型PRNG）を導入し、**ゲームプレイに影響する
  乱数だけ**をシード経由に差し替える: `populate()` のパターン抽選・座標、
  ゴールドコイン判定。雪・スプレーなど視覚だけの乱数は `Math.random` のまま。
  ※G1-2のpopulate改修と同時にやると二度手間がない。
- モード2つ: **FREE RIDE**（従来通りランダム）と **DAILY RUN #N**
  （シード=UTC日付。世界中が同じコース。Nは適当な起点日からの通算日）。
  スタート画面にモード選択を追加。デイリーベストは `snowline-daily-YYYYMMDD` に保存。
- リザルト画面に**SHAREボタン**: `navigator.share`、無ければクリップボードコピー。
  テキスト例:
  ```
  SNOWLINE DAILY #214
  ⛷️ 1,247m  💨 178km/h
  🌲ニアミス×12 ⭐フィーバー×2 🏔️雪崩から逃走 892m
  https://…/?daily
  ```
  数字と絵文字だけで「俺の走り」が伝わり、貼られた側は同じコースで挑戦できる。
- URLパラメータ `?seed=` で任意コースへの挑戦リンクも可能にする。

**完了条件**: 同じ日付なら誰の環境でも同一の障害物配置になる（2ブラウザで検証）。
シェアテキストがワンタップでコピーされる。

## W3: 最初の30秒 + 国際化

- **FTUE**: 初回プレイ検出（localStorage）時、最初の250mは障害物密度50%・
  コイン多め・ランプ1基保証。初ニアミス時にトーストで「ニアミスで加速！」等の
  ワンライン教育（チュートリアル画面は作らない。読まれないので走りながら教える）。
- **国際化**: UI文字列とボイスを `navigator.language` で日英切替
  （文言テーブル1個。`announce()` のlang/テキストも切替）。UIはすでに英語主体
  なので実質ボイスとトースト・READMEのみ。
- リトライ摩擦の最終確認: ゲームオーバー→再走が1操作（R/Enter/ボタン）で
  1秒以内に始まること。

## W4: PWA + 低スペック自動調整

- `manifest.json` + Service Workerで**インストール可能・オフライン動作**に。
  Google Fontsのセルフホスト化（第1弾バックログ）もここで一緒に解消する。
- **自動画質調整**: プレイ開始5秒間の平均fpsを計測し、45fps未満なら
  pixelRatio→1・影オフ（`sun.castShadow = false`）・雪パーティクル半減。
  判定結果は保存して次回から適用。世界の主戦場は中低スペックAndroidである。

## チューニング指針（第3弾分）

| 項目 | 目標 | 意図 |
|---|---|---|
| 雪崩との距離 | 通常プレイで60〜150mを行き来 | 常に「気配」がある |
| 雪崩に捕まる死 | 全死因の20%程度 | 主因は障害物のまま |
| BGMレイヤー全開時間 | 1ランの20〜30% | ダイナミクスを残す |
| シェア文言 | 3秒で読めて数字が2個だけ目立つ | コピペされる条件 |

## 壊してはいけないもの（第3弾追加分）

- シード乱数導入時、**FREE RIDEの手触りを変えないこと**（PRNG差し替えで
  分布が変わっていないか、体感プレイで確認）。
- BGM追加後も効果音の聴感優先度が上であること（SEがBGMに埋もれたら本末転倒。
  BGMのmasterへのゲインは控えめに始める）。
- 雪崩の恐怖演出もカメラ禁止事項（ロール・ランダム揺れ・ズーム脈動）は厳守。

## 備考（コード外の話。ユーザー向けメモ）

配信チャネル（itch.io / CrazyGames / Poki への掲載、クリップ素材の用意）は
コードの外の仕事なので本指示書の範囲外だが、W0〜W2が揃った時点が公開の適時。

---
---

# 第4弾: リアリティ（世界の説得力）

## プロデューサーレビュー: リアリティとは何か

先に定義を共有する。ゲームにおけるリアリティは**フォトリアルのことではない**。
必要なのは次の2つだ。

- **反応性**: 世界がプレイヤーの行為に反応すること。雪の上を滑ったのに跡が
  残らない世界を、脳は「本物」と認めない。
- **一貫性**: 様式が揃っていること。現状は写真の背景・写真の木・写真のスキーヤーの
  「フォトコラージュ様式」に、ローポリのフラットシェーディング岩が混在している。
  様式の破れは1箇所でも全体を安っぽく見せる。
  （なおコイン等のピックアップがゲーム的な見た目なのは万国共通の記号なので許容。
  **世界に属する物=様式厳守、UIに属する物=ゲーム的でよい**、が線引き）

コード上の確認済みの穴:
- シュプール（滑走跡）が存在しない — 反応性の欠如として最大
- 地形の起伏が速度に影響しない、スキーヤーが斜面にピッチ追従しない
  （`skier.rotation` はz=リーンとy=ヘディングのみ）
- `scene.background` が静止画直貼りで、左右移動(±26)しても背景が1pxも動かない
- コース幅±26の見えない壁に視覚的説明がない（リアリティ兼UXの欠陥）
- 写真素材（背景・木・スキーヤー）が `MeshBasicMaterial / toneMapped: false` で
  ライティングに一切反応しない=「貼り付け感」の正体

## 着手順序の更新（最終版マスタープラン）

**G0〜G2 → R0 → W2(BGM) → W1(雪崩) → R1 → W0(シェア) → W3 → W4**、R2は公開後。

R0は爽快感Gシリーズの完成に不可欠（自分のラインが雪面に見える＝パンプの教材）。
R1は**公開前に必須**。W0のシェアで人が来た瞬間のスクリーンショットこそが拡散の
本体であり、書き割りのスクショでは二次拡散が起きない。

---

## R0: 反応する雪面（Gシリーズと同格の最優先）

### R0-1: シュプール（滑走跡）
- 左右のスキーそれぞれの通過点をリングバッファに記録（進行0.4mごとに
  `{worldZ, x, heading}` をサンプル、直近150m分=約400点）。
- 毎フレーム、障害物と同じ座標変換（`z = 4 - (worldZ - progress)`）で
  2本のリボンジオメトリ（BufferGeometry、三角形ストリップ）を再構築。
  幅0.09、地面+0.01に浮かせ、`MeshBasicMaterial`（薄い青灰 0x9db8c2、
  transparent、古い区間ほどopacityフェード、depthWrite: false）。
- カービング中はエッジが立つので跡を濃く・わずかに幅広に（`|edgeAngle|` 連動）。
- `reset()` でバッファをクリアすること。ジオメトリは1個を使い回し
  `needsUpdate` で更新（毎フレーム `new` は禁止）。
- **効果はリアリティに留まらない**: 自分の描いたS字が雪面に残る＝G0-1パンプの
  上達が目に見える。爽快感システムの最後のピース。

### R0-2: 地形と身体の整合
- **斜面→速度の結合**: プレイヤー位置の局所勾配
  `slope = (terrainHeight(playerX, progress+1) - terrainHeight(playerX, progress-1)) / 2`
  を毎フレーム計算し、基準勾配(-0.16)との差分を `boost` に加減算
  （係数は小さく始める。ローラーの登りで伸びが鈍り、越えた瞬間に伸びる、が体感目標）。
  G1-1のクレストジャンプと合流し「地形を読む」が実利になる。
- **ピッチ追従**: 同じ勾配から `skier.rotation.x` を算出してゆっくりlerp。
  写真スプライトごと傾いてよい（微小角なので破綻しない）。
- **着地の雪煙**: 着地時に既存スプレー系で放射状バースト（粒数は滞空時間比例）。
  「PERFECT LANDING」の視覚的裏付けになる。

**完了条件**: 後ろを振り返らなくても、S字に滑った跡が画面内の雪面に残って
流れていくのが見える。ローラー地形で速度が呼吸する。

## R1: 生きているコース（公開前に必須）

### R1-1: コースの実在感（セットドレッシング）
- **オレンジの安全ネット**を `|x| ≈ 26` 付近に断続配置（棒2本+半透明ネット地の
  繰り返し。共有ジオメトリ・共有マテリアル）。スキー場の記号として世界共通で
  伝わる上、**見えない壁だった±26の境界がゲーム的にも説明される**。
- **リフト鉄塔+架線**: 200〜400mに1回、コースを横切る頭上のケーブルと鉄塔。
  遠景のゴンドラは動かなくてよい（動くと視線を奪う）。
- **圧雪車の縞（コーデュロイ）**: `makeSnowTexture()` に細かい縦縞を加える。
  ゲレンデの「今朝圧雪しました」感は1関数の修正で出る。費用対効果最大の一手。
- 斜度標識・コース番号看板を低頻度で（既存HUDの座標表示と世界観が繋がる）。

### R1-2: 光と天候のドラマ
- 走行距離で天候フェーズを緩やかに遷移: 快晴 → 降雪強め → 一瞬の吹雪の気配 →
  回復。変えるのは降雪パーティクル密度/速度・fog密度（**±20%以内**。視界の
  公平性を守る）・風SE。吹雪の前には風音で2秒予告する（理不尽の回避）。
- 長いラン（2000m+）で太陽色をゴールデンアワー方向へゆっくりlerp。
- **写真素材のライティング応答**: 背景・木・スキーヤーの各マテリアルの `color` を
  太陽色・強度に連動して毎フレーム微tintする（1マテリアル1行）。これだけで
  「貼り付け感」が大幅に減る。フォトコラージュ様式のまま光に馴染ませる、が方針。
  ※スキーヤーを隠し3Dモデル（`createSkier()` は既に存在する）に差し替える案は
  R2でA/B判定する。今は様式を写真側に寄せて統一する。
- **背景のパララックス**: `scene.background` 直貼りをやめ、遠景に大きな湾曲
  プレーン（またはシリンダー片）として写真を配置し、`playerX * 0.02` 程度だけ
  逆方向にオフセット。左右に振ったとき遠景がわずかに流れるだけで世界に奥行きが出る。
  fog色と背景の境界が馴染むかを必ず目視確認すること。

### R1-3: 様式の破れの修繕
- 岩: フラットシェーディングのままでもよいが、雪帽り（上面に白い
  ジオメトリを被せる or 頂点カラーで上面を白く）を追加し、彩度をコース側に寄せる。
- ランプ: 人工物なので現状の見た目でよいが、雪の付着（上面の白帯）を足す。

**完了条件**: 任意の瞬間のスクリーンショットが「スキー場の写真」として
SNSに貼れるレベルに見えること（これがW0シェアの弾になる）。

## R2: 公開後の深化（バックログ）

- **他のスキーヤー**: 前方を予測可能なラインで滑るNPC（1〜2人、決して隣接
  スポーンしない、ニアミス2倍）。世界が「営業中のゲレンデ」になる最強の生命感。
  公平性の検証が重いので公開後に。
- **雪質ゾーン**: パウダー（減速+大スプレー+跡が深い）/ アイスバーン（グリップ低下
  +カービング音が変わる）。リアリティとゲーム性の交点だがバランス影響大。
- **スキーヤー表現のA/B**: 写真スプライト vs 既存3Dモデル(`createSkier()`)を
  トグルで切替可能にして被験者テスト。
- **音響空間**: 谷の残響（Convolver+生成インパルス）、遠くのリフト駆動音、
  雪質連動のカービング音。

## チューニング指針（第4弾分）

| 項目 | 目標 | 意図 |
|---|---|---|
| シュプールの視認性 | 意識すれば見える、意識しないと邪魔にならない | 反応性>主張 |
| 斜面→速度の結合係数 | ローラーで±8%程度の呼吸 | リアリティ>理不尽 |
| fog/天候の変動幅 | ±20%以内 | 公平性の死守 |
| ネット/鉄塔の頻度 | 「たまに横目に入る」 | 密度を上げすぎない |

## 壊してはいけないもの（第4弾追加分）

- 60fpsの死守。シュプール・ネット・鉄塔はすべて共有ジオメトリ+使い回しで実装し、
  実装ごとに `renderer.info.render.calls` の増分を確認（各機能+10call以内が目安）。
- 判定・操作系には一切触れない（見た目の追加が当たり判定を持ってはならない。
  ネットも鉄塔も衝突しない。壁の実体は従来通り±26のclampのみ）。
- 天候・光の変化がプレイの公平性を侵さないこと（視界を奪う演出は予告付きで短く）。
- カメラ禁止事項は引き続き厳守。パララックスは背景側を動かす（カメラは動かさない）。
